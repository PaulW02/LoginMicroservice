name: kthcloud-ci
"on":
  push:
    branches:
      - master
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: registry.cloud.cbh.kth.se
          username: PaulW02
          password: Adda2002!
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: registry.cloud.cbh.kth.se/paulw02/login-microservice-image:latest
