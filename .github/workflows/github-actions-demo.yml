name: kthcloud-ci
"on":
  push:
    branches:
      - master
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: paulw02
          password: Adda2002!
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: paulw02/login-microservice-image:latest
